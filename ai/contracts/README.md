# AI Contracts

Use the JSON example in:

```text
../../contracts/ITINERARY-DRAFT.json
```

The backend schema is the authoritative input shape.

When changing the AI schema, coordinate with Mustafa before changing backend validation.
